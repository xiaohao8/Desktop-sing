# 微软商店上传：准备与步骤（操作手册）

> **与 `README-STORE.md` 的分工**：那份讲「**为什么 / 是什么**」——商店版行为差异、
> 13 条政策逐条对照、常见拒审原因、踩过的坑。这一份只讲「**按什么顺序做什么**」，
> 是从零到上架的线性操作清单。两份互相引用，不重复。
>
> 目标产物：`store/out/Desktop-sing-<x.y.z.w>-x64.msix`（**未签名**，提审就传未签名包）+
> `store/out/listing-v<版本>.md`（提审文案，复制粘贴用）。

---

## 0. 现状速览（2026-09-19 17:50 更新）

| 环节 | 状态 | 说明 |
|---|---|---|
| Partner Center 开发者账号 | ✅ **已注册** | 用户已完成 |
| 应用名预留「桌面歌词」 | ✅ **已预留** | 名称须与清单 `DisplayName` 一致 |
| `store/identity.local.json` | ❌ **未创建** | 缺 Partner Center「产品标识」里的两行；当前出的是**占位标识包，不能上传** |
| MSIX 产物 | ⚠️ **占位包** | `store/out/Desktop-sing-1.0.0.0-x64.msix`（234 文件 / 45.8 MB），填好标识后**必须重出** |
| 提审文案 | ✅ 已就绪 | `store/out/listing-v1.0.0.md`；配好 `site_url` 后重跑会自动填上两个真实隐私政策地址 |
| 离线自检 | ✅ 117 项，**FAIL 0**；WARN 2 | 两条 WARN 就是本节的两个未决项（`P5.20a` 包标识、`P5.20b` 站点地址），**上传前必须清零** |
| 站点 + 隐私政策页 | ✅ 仓库已就绪 | `https://github.com/xiaohao8/music`（30 文件，坏引用 0），待 Netlify 部署出域名 |
| 隐私政策 URL | ⏳ **等部署** | 中文 `/privacy.html`、英文 `/privacy.en.html` |
| 商店截图 | ⚠️ 有，但**非真机截屏** | 3 张 1600×900 离屏合成图（§1.5） |
| WACK 认证 | ⚠️ 未跑 | 需管理员权限，非必需但建议 |

**四条硬门槛**：**① 开发者账号 ✅ → ② 名称预留 ✅ + 包标识回填 ❌ → ③ 隐私政策 URL 发布 ⏳ → ④ 截图 ⚠️**。

> ⚠️ **别拿 `store/out/` 里现成的那个 `.msix` 去上传** —— 它是占位标识包（包内
> `Identity Name="Desktop-sing-PLACEHOLDER"`），微软那边包名对不上会直接拒。
> 填好标识重出包之后，`audit_round3.py` 的 `P5.20c` 会替你守住这条线：本地标识填了、
> 包还是旧的话它会 **FAIL**。


---

## 1. 准备（一次性）

### 1.1 注册 Partner Center 开发者账号

1. 打开 <https://partner.microsoft.com/dashboard>，用**长期使用的邮箱**注册
   （这个邮箱以后是审核沟通入口，别用小号）。
2. 选「个人」或「公司」开发者。个人账号注册费低、验证简单，本应用是免费工具，
   个人账号足够。
3. 完成身份验证（可能需要身份证件/企业资质），**审核 1~3 个工作日**，
   这一步是整个流程里最长的等待，越早开始越好。

### 1.2 预留应用名称

1. Partner Center → 「创建新应用」→ 名称填 **`桌面歌词`**。
2. 名称被占用时试变体：`桌面歌词 Desktop-sing`、`桌面歌词 Lite` 等。
   预留后名称会被保留一段时间，期间别人占不走。
3. **这个名称就是商店里显示的商品名**，请与清单里的 `DisplayName`（`桌面歌词`，
   在 `store/build_store.py` 的 `DISPLAY_NAME`）保持一致 —— 元数据与实际产品不符
   是 10.1.1 的典型判罚点。

### 1.3 抄回包标识，写入 `store/identity.local.json`

1. 应用 → 「产品管理」→「产品标识」，复制两行：
   - **Package/Identity/Name**，形如 `12345Desktop-sing`
   - **Publisher**，形如 `CN=1A2B3C4D-5E6F-...`
2. ⚠️ **必须原样复制，不能手敲**。Publisher 是微软签发证书的主题串，
   **逐字符匹配**，错一个字符包就装不上（报证书与清单不匹配）。
3. 写入本机标识文件（**勿提交、勿外传**，`.gitignore` 已拦）。
   顺手把 §1.4 的**站点根地址**也写进去 —— 出包时会打印出来、`--listing` 会自动
   把两个隐私政策地址填进提审文案，省得到时手拼：

   ```json
   {
     "name": "12345Desktop-sing",
     "publisher": "CN=1A2B3C4D-5E6F-...",
     "site_url": "https://xxx.netlify.app"
   }
   ```

   `site_url` 末尾带不带 `/` 都行（会归一化掉）。三项都可以临时用命令行覆盖：
   `--name` / `--publisher` / `--site-url`。

4. 验证是否生效 —— 重新出包，控制台**不应**再出现占位警告：

   ```bash
   .buildenv\Scripts\python.exe store\build_store.py
   ```

   ```
   标识   : 12345Desktop-sing          ← 应为你自己的值
   发布者 : CN=1A2B3C4D-...            ← 应为你自己的值
   隐私页 : https://xxx.netlify.app/privacy.html
   ```

   若打印 `Desktop-sing-PLACEHOLDER` / `CN=PLACEHOLDER-FILL-ME`、`隐私页 : （未配 site_url…）`，
   或末尾出现「⚠️ 当前是占位标识：上传前必须填真实 Identity」，说明文件没读到。

   再跑一次自检，`P5.20a` / `P5.20b` 的两条 WARN 应该消失；
   **没重出包之前 `P5.20c` 会 FAIL**（本地标识填了、`out/` 里还是占位包）。

   > 优先级：命令行 `--name/--publisher/--site-url` → `identity.local.json` → 占位。
   > 临时试包可以用命令行传参，但**正式提审建议落文件**，免得手滑漏参数发出占位包。


### 1.4 发布官网，拿到隐私政策 URL

**这一条是政策硬要求**：商店 10.5.1 特别点名 **Desktop Bridge 与 Win32 产品必须始终具备
隐私政策**，缺 URL 直接拒审；而且 URL **必须长期有效**（下架前后的审核都可能来访问）。

仓库里的两份声明已就绪，只差一个公网地址：

| 文件 | 用途 |
|---|---|
| `site/privacy.html` | **中文 listing** 的隐私政策 URL |
| `site/privacy.en.html` | **英文 listing** 的隐私政策 URL（商店是全球分发的） |

做法：

- **现状（已搭好，走这条）**：站点内容已单独放成**独立部署仓库**
  <https://github.com/xiaohao8/music>（主仓库 `site/` 的部署副本，30 个文件、
  坏引用 0）。在 Netlify 里 **Import an existing project → 选 `music`** 即可，
  仓库里的 `netlify.toml` 已写死 `publish = "."`、**无构建命令**，构建设置不用动。
  拿到形如 `https://<站点名>.netlify.app` 的域名后，写进 `identity.local.json` 的
  `site_url` 并重跑 `build_store.py --listing`，文案里的两个地址会自动填好。
  > 站点源文件永远以主仓库 `site/` 为准：改完跑 `python sync_site.py --apply --push`
  > 同步（`--check` 只校验）。**`privacy.html` / `privacy.en.html` 这两个文件名不要改**，
  > 改了等于让商店表单里的链接失效。有自定义域名优先用自定义域名（比 `*.netlify.app` 更可信）。
- **备选：自有域名静态托管**：把 `site/` 整个上传即可，注意保留 `assets/` 目录。
- **备选：GitHub Pages**：`xiaohao8/Desktop-sing` 是公开仓，Settings → Pages →
  选 `main` 分支的 `/site` 目录，得到形如
  `https://xiaohao8.github.io/Desktop-sing/privacy.html`。
- 国内访问不稳的话，**别用蓝奏云等文件分享页当隐私政策 URL** —— 那类页面有广告插页、
  链接会变，审核会认为不可靠。

发布后**必须实测**：无痕窗口打开两个 URL，均返回 200、无登录墙、正文可读
（中文页有 8 节、英文页对应同序章节）。

> **页面本身已经验过了（2026-09-19，无头 Edge 真实加载）**，所以线上只需确认「地址打得开」：
>
> | 检查 | 结果 |
> |---|---|
> | 三页渲染 | `index.html` 2593 字 / `privacy.html` 1905 字 / `privacy.en.html` 5409 字，CSS 生效、深色底正常 |
> | 图片与资源 | 15 + 1 + 1 张图，**0 张坏图、0 个失败请求、0 个 4xx**（相对路径大小写在真实浏览器里全对） |
> | 控制台 | 三页均**无异常** |
> | 首页滚动入场 | 33 个 `.reveal` 装在「进视口才点亮」逻辑上：首屏本来就没有隐藏内容，滚到底后 **33/33 全部点亮** |
> | 中文隐私页结构 | 8 节齐全（存储 / 读取 / 联网 / 不做的 4 件事 / 第三方 / 儿童 / 变更 / 联系） |
>
> 截图留档：`store/out/site-render/privacy-render-check.png`（1440×3272，`store/out/` 不进仓库）。

### 1.5 准备商店截图

**现成素材**（`store/out/shots/`，3 张 **1600×900**，已超过商店建议的 1366×768）：

| 文件 | 内容 |
|---|---|
| `store-shot-1-idle.png` | 等待播放态 —— 审核员装完第一眼看到的画面 |
| `store-shot-2-hero.png` | 有歌词时的浮层，含逐字高亮 |
| `store-shot-3-settings.png` | 设置面板（体现功能丰富度） |

重新生成（清过产物、换机器、或改了 UI 之后都要跑）：

```bash
.buildenv\Scripts\python.exe store\render_shots.py
```

> ⚠️ **这三张是离屏合成图**（真实控件渲染 + 合成壁纸），**不是真机截屏**。
> 提审**建议再补 1 张真机桌面截图**：审核员和用户都更容易信服，观感也更真实。
> 真机图的拍法要求见本节末尾三条注意事项。

> ⚠️ **截图属对外发布物**：只用自写占位歌词，**不要**出现真实歌曲歌词、
> 第三方播放器界面或其他受版权保护的素材。（这条已被审计用禁词扫描锁住。）

如果换成真机截图，注意三点：
1. 分辨率 ≥ 1366×768（1920×1080 最稳）；
2. 壁纸用纯色/自备图，画面里不要出现第三方播放器的界面元素；
3. 画面里出现的歌曲名/歌词用自己造的示例内容。

### 1.6 定价与市场

- **定价**：免费。
- **市场**：全球（界面目前只有简体中文，但英文 listing 也要填 —— 见 §4）。
- **支持设备**：PC。清单 `MinVersion` 为 17763（Windows 10 1809），x64。

---

## 2. 每次提审：出包

```bash
# ① 改版本号（lyrics_overlay.py 的 APP_VERSION，如 "1.0.0" → "1.0.1"）
#    ⚠️ 商店版每次提审版本号必须**大于**上次，不能重发同一版本号
# ② 离线自检（清单完整性 + 商店模式运行时 + 隐私政策一致性 + 截图素材）
.buildenv\Scripts\python.exe store\audit_round3.py
# ③ 全量重建并打包（提审前一定用 --fresh，保证产物 = 当前代码）
.buildenv\Scripts\python.exe store\build_store.py --fresh
# ④ 刷新提审文案（认证说明里会写版本号）
.buildenv\Scripts\python.exe store\build_store.py --listing
```

**通过判据**（逐条核对，任何一条不对都别上传）：

| 检查点 | 期望 |
|---|---|
| ① 自检 | **FAIL: 0**（当前共 117 项）。WARN 也要为 0 —— 若还有 `P5.20a`（包标识）或 `P5.20b`（站点地址）的 WARN，说明 §1.3 / §1.4 还没做完，**先补完再上传** |
| ② 标识行 | 打印的是 Partner Center 的真实 `name` / `publisher`，且 `隐私页` 行不是「（未配 site_url…）」，**无 PLACEHOLDER 警告** |
| ③ 产物名 | `store/out/Desktop-sing-<四段版本>-x64.msix`，版本必须是 `APP_VERSION` 补零成四段 |
| ④ 包内校验 | 控制台有「`[OK]` 清单引用的 N 个资产全部就位」「`[OK]` 包内 N 个文件」 |
| ⑤ 打包警告 | 不应出现 `error:` / `warning:` 行 |
| ⑥ 产物清单 | `store/out/listing-v<版本>.md` 已刷新（日期无关，看版本号 + §8 里的隐私地址） |

> ⚠️ **沙箱/本机都可能遇到的坑**：`build_store.py` 要覆盖已存在的 `.msix` 时，
> 本机安全策略可能拦批量删除（返回 `SAFE_DELETE_BULK_CONFIRM_REQUIRED`，
> **退出码 1 但 exe 已经建完**）。处理：先把旧 `.msix` 移进回收站再重跑，
> 或直接删掉旧文件。判定产物新旧**只看文件名里的版本号与 mtime**。

---

## 3. 每次提审：本地验收

### 3.1 离线自检（必做，1 分钟）

```bash
.buildenv\Scripts\python.exe store\audit_round3.py
```

它强制 `STORE_MODE=True`、真实实例化浮层与设置面板并走一遍 `refresh()`，
同时核对：清单与资产完整性、随包文档（中英 5 份）、隐私政策与代码行为一致性、
对外文档口径（不含逆向细节）、商店截图素材。**全过再上传，心里有底。**

### 3.2 WACK 认证（建议，需管理员）

```bash
.buildenv\Scripts\python.exe store\build_store.py --wack
```

`appcert.exe` 就是商店审核用的那套测试集，本地过了基本不会栽在技术认证上。
**非管理员环境会 `Permission denied`，属预期，不是代码问题** —— 换管理员终端再跑。

### 3.3 自签安装 + 商店模式行为核对（强烈建议）

未签名的 MSIX 本机装不上，需临时自签（完整命令见 `README-STORE.md` §3）：

```powershell
# Subject 必须与清单 Publisher 逐字符一致
$cert = New-SelfSignedCertificate -Type Custom -Subject "CN=你的Publisher" `
  -KeyUsage DigitalSignature -FriendlyName "Desktop-sing 本地测试" `
  -CertStoreLocation "Cert:\CurrentUser\My" -TextExtension @(
    "2.5.29.37={text}1.3.6.1.5.5.7.3.3", "2.5.29.19={text}")
signtool sign /fd SHA256 /sha1 <证书指纹> store\out\Desktop-sing-<版本>-x64.msix
Export-Certificate -Cert $cert -FilePath test.cer
Import-Certificate -FilePath test.cer -CertStoreLocation "Cert:\LocalMachine\TrustedPeople"
Add-AppxPackage -Path store\out\Desktop-sing-<版本>-x64.msix
```

装上后**逐条核对商店版行为**（这些正是审核员会点的位置）：

- [ ] 托盘菜单「开机自启」跳转到系统「启动应用」设置页（不是直接改注册表）；
- [ ] 托盘菜单**没有**「进程保活」项；
- [ ] 设置面板：系统卡片是「打开启动设置」按钮、更新区是商店提示文字
      （**无更新地址输入框、无「立即检查更新」**）；
- [ ] 「检查更新…」提示由商店分发；
- [ ] 「关于」窗口**不含**「进程保活」字样、不虚报能力；
- [ ] 歌词、逐字、屏保、快捷键等主功能与普通版一致；
- [ ] **卸载**：系统设置 → 应用 → 卸载，卸载后 `%APPDATA%\Desktop-sing`
      的包私有视图被系统一并清理。

---

## 4. 提交（Partner Center 逐项填）

> 页面文案可能微调，**以实际页面为准**；字段内容一律从
> `store/out/listing-v<版本>.md` 复制，别手打。

| 页面 / 字段 | 填什么 | 来源 |
|---|---|---|
| **Packages** | 上传 `store/out/Desktop-sing-<版本>-x64.msix` | **未签名包**，微软自己重签 |
| **Properties → 类别** | 音乐（Music）；次级：实用工具 | listing §2 |
| **Properties → 隐私政策 URL** | 中文 listing 填 `site/privacy.html` 的地址；**英文 listing 填 `privacy.en.html` 的地址** | §1.4 发布后的真实 URL |
| **Properties → 支持联系方式** | 邮箱（建议用注册账号的邮箱）+ 官网地址 | —— |
| **Properties → 系统要求** | Windows 10 1809 及以上 / Windows 11，x64 | listing §2 |
| **Age rating（IARC 问卷）** | 如实填：无用户生成内容、无社交、无付费、无暴力色情 → 通常 PEGI 3 / Everyone | listing §10 |
| **Store listing → 描述** | 粘贴全文（含【使用前提（重要限制）】段） | listing §3 |
| **Store listing → 搜索词** | 7 个：桌面歌词、歌词、悬浮歌词、卡拉OK、逐字歌词、桌面工具、音乐 | listing §4 |
| **Store listing → 截图** | 上传 `store/out/shots/` 三张（+ 真机图） | §1.5 |
| **Store listing → 图标** | 300×300 PNG：用根目录 `icon.png`（1024×1024）等比缩到 300×300 导出；也可用 `store/assets/Square310x310Logo.png`（310×310）转存 | 页面若带裁剪工具，直接传 `icon.png` 让它裁 |
| **Store listing → 语言** | **只声明 `zh-CN`** | 声明几种就要本地化几种描述；虚报 en-US 等于声明一个不存在的本地化 |
| **版本说明（What's new）** | **首次提交留空** | listing §5 |
| **提交选项 → 受限功能说明** | **只填 listing §6 那两句话** | ★ 见下方提醒 |
| **提交选项 → 认证说明** | 粘贴 listing §7 全文 | ★ 见下方提醒 |
| **提交选项 → 发布方式** | 「认证通过后尽快发布」 | 便于发现问题及时处理 |

**★ 两条必须留意的地方：**

1. **受限功能说明**：上传包后页面会提示「需要请求批准才能使用受限功能 `runFullTrust`」
   —— 这是**正常预警，不是报错**，所有 Tauri / Electron / Win32 转 MSIX 的应用都会触发，
   不需要提前单独申请。**该字段有长度限制，长文案会被静默截断**，
   所以只填 listing §6 给的两句话。
   另注：`internetClient` 对 full-trust 桌面应用**不需要**，本清单只声明 `runFullTrust`。
2. **认证说明**：认证的本质是**合规审查，不是功能审查** —— 审核员**不会逐个验证功能是否正常**，
   只验证签名、清单合法性、能力声明、政策合规、启动不崩溃。
   所以必须写清**怎么测**。本应用最关键的提示是：
   **「播放器必须接入 Windows 系统媒体控制（SMTC），歌词才会出现」**，
   以及**全局快捷键默认关闭**（审核员按 Ctrl+Alt+S 没反应会误判功能异常）。
   这两条 listing §7 里都写了，**别删**。

---

## 5. 认证与发布

1. 提交后状态依次：`预处理中 → 认证中 → 发布中 → 已发布`。
   认证通常几小时到 1~2 天，**首次提交往往更久**。
2. **认证失败**：在提交报告里能看到具体条款号与描述。对照 `README-STORE.md` §9
   「常见拒审原因对照」定位 → 改文案/改代码 → **版本号递增** → 重出包 → 重新提交
   （见 §2 全流程）。
3. **认证通过后**商店自动向用户推送更新，不需要用户操作，也不需要我们做任何事。
4. 想暂停发布或回滚：Partner Center 可以把某次提交「停止发布」，
   但**已安装的用户不会被远程卸载**，只能发新版本修正。

---

## 6. 发布之后

- [ ] 商店页自查：描述排版正常、截图显示正常、隐私政策链接能打开；
- [ ] 换一台**没装过开发环境**的机器（或另一个账号）从商店装一次，
      确认能装、能显示歌词；
- [ ] 确认商店版**没有**任何自更新入口（应该看不到更新地址输入框）；
- [ ] 记下发布版本号，下次提审在这个号上递增。

**发新版**（最短路径）：

```bash
# ① 升 APP_VERSION  ② audit_round3.py  ③ build_store.py --fresh
# ④ build_store.py --listing（认证说明里的版本号要跟着变）
# ⑤ Partner Center → 该应用 → Packages → 上传新 MSIX → 提交
```

---

## 7. 卡点速查

| 现象 | 原因 | 处理 |
|---|---|---|
| 出包打印 `PLACEHOLDER` / 上传后被拒 | 没填 `store/identity.local.json` | §1.3 |
| MSIX 装不上，报证书/发布者不匹配 | 自签证书 Subject 与清单 `Publisher` 不一致 | 逐字符对齐，勿手敲 |
| 重打包报 `SAFE_DELETE_BULK_CONFIRM_REQUIRED`（exit 1） | 覆盖旧 `.msix` 被安全策略拦 | 先把旧文件移回收站/删掉再重跑 |
| `makeappx` 报找不到资产 | 清单引用了包里没有的图标 | 跑 `store/make_store_assets.py` 重新生成资产，再 `build_store.py` 出包（出包时会自检清单引用的每个资产是否都在） |
| WACK `Permission denied` | 不是管理员 | 管理员终端再跑 |
| 审核说「描述不准确」 | 描述与实际功能不符 | 逐条核对 listing §3 的限制段；界面语言、SMTC 前提、系统版本三项最容易被挑 |
| 审核说「测不出功能」 | 审核员不知道要接播放器 | listing §7 认证说明必须写清 SMTC 与快捷键默认关闭 |
| 审核说「缺隐私政策」 | URL 没填或打不开 | §1.4，注意 URL 必须长期有效 |
| 提交后提示受限功能需批准 | `runFullTrust` 正常预警 | 填两句话说明即可，不阻碍提交 |
| 版本号被拒 | 与上次相同 | 版本号必须递增（四段） |

---

## 附录 A：命令速查

```bash
# 离线自检（117 项；上传前要求 FAIL 0 且 WARN 0）
.buildenv\Scripts\python.exe store\audit_round3.py

# 从源码全量重建 + 打包 MSIX
.buildenv\Scripts\python.exe store\build_store.py --fresh

# 只重新出包（跳过重建 exe，改过 store/ 内文件时用）
.buildenv\Scripts\python.exe store\build_store.py

# 只刷提审文案（认证说明里的版本号 + 隐私政策地址）
.buildenv\Scripts\python.exe store\build_store.py --listing

# 重新生成商店截图（1600×900 ×3）
.buildenv\Scripts\python.exe store\render_shots.py

# 出包并跑 WACK（需管理员）
.buildenv\Scripts\python.exe store\build_store.py --wack

# 站点同步到部署仓库（改过 site/ 之后）
python sync_site.py --check              # 只校验，有漂移退出码 1
python sync_site.py --apply --push        # 同步并推送（Netlify 自动重新部署）
```

## 附录 B：现在还剩什么没做

| # | 待办 | 属于 | 备注 |
|---|---|---|---|
| 1 | 注册 Partner Center 账号 | ✅ 已完成 | —— |
| 2 | 预留名称「桌面歌词」 | ✅ 已完成 | —— |
| 3 | **抄包标识写进 `store/identity.local.json`** | 人工（Partner Center → 产品管理 → 产品标识） | 拿到后重跑 `build_store.py`，自检 WARN 归零 |
| 4 | **把站点部署到 Netlify，拿到域名并填 `site_url`** | 人工（Import `xiaohao8/music`） | 之后重跑 `--listing` 自动填两个隐私地址 |
| 5 | 补 1 张真机桌面截图（可选） | 人工 | 现成 3 张为离屏合成图 |
| 6 | 跑一次 WACK（可选，需管理员） | 人工 | —— |
| 7 | 提交时填受限功能说明 + 认证说明 | 复制 listing §6 / §7 | 内容已备好 |

> 3 和 4 做完之后：`build_store.py --fresh` → `--listing` → `audit_round3.py`
> 三项全绿（**FAIL 0 / WARN 0**），就可以把 `.msix` 传上去了。
